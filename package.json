{
  "name": "hometale",
  "private": true,
  "workspaces": [
    "cli",
    "server",
    "web"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "cli": "cd cli && npm run start",
    "pack": "./scripts/prepublish.sh && cd cli && npm pack"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  },
  "packageManager": "npm@10.0.0"
}
